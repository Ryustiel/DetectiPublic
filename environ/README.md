# Environment

Copy each `.env.example` file you need to the matching `.env` filename.

- `vision.env`: runtime settings for the vision API.
- `gdinoonnx.env`: GroundingDINO ONNX/TensorRT build settings.
- `tunnel.env`: Cloudflare tunnel token.

The real `.env` files are ignored by git.
